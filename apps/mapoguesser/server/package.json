{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "publish:maincloud": "spacetime publish mapoguesser-stats -s maincloud -y --module-path .",
    "publish:local": "spacetime publish mapoguesser-stats -s local -y --module-path .",
    "generate:client": "spacetime generate --lang typescript --out-dir ../client/src/module_bindings --module-path ."
  },
  "dependencies": {
    "shared": "*",
    "spacetimedb": "^2.2.0"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
