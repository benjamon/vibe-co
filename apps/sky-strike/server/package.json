{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "publish:maincloud": "spacetime publish ss-hs-70fxp -s maincloud -y --module-path .",
    "publish:local": "spacetime publish ss-hs-70fxp -s local -y --module-path .",
    "generate:client": "spacetime generate --lang typescript --out-dir ../client/src/module_bindings --module-path ."
  },
  "dependencies": {
    "shared": "*",
    "spacetimedb": "2.1.0"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
