node_modules/
dist/
dist-itch/
*.zip
*.tsbuildinfo

# Tauri
src-tauri/target/

# Capacitor
android/.gradle/
android/app/build/
ios/App/Pods/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Generated SpacetimeDB client bindings — committed so production builds and
# fresh clones can connect without re-running `spacetime generate`. Regenerate
# (and commit) whenever the server schema in `server/src/index.ts` changes.
